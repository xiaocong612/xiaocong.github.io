# 小聪的记录：项目进度

更新时间：2026-09-12

## 下次如何继续

下次用户说“继续”时，先读取本文件和 `docs/superpowers/specs/2026-09-06-personal-hexo-blog-design.md`、`docs/superpowers/plans/2026-09-06-personal-hexo-blog.md`，再从“尚未完成”开始执行。不要重新搭建主题或重复已经完成的实现。

## 当前目标

完成一个中文个人 Hexo 博客：本地 Markdown 写作为主，同时保留完整的 Decap CMS 后台；视觉采用原创的 Apple 风格设计语言，支持浅色和深色模式；通过 GitHub Actions 发布到 GitHub Pages，并预留 Cloudflare Worker GitHub OAuth。

## 已完成

- 已将默认 Hexo 站点改造成中文博客，站点名为“**小聪的记录**”。
- 已完成主题 `themes/apple-journal`：中文导航、精选文章、文章流、文章页、归档、分类、标签、关于页、本地搜索、响应式布局、系统深色模式。
- 已完成内容模型：文章分类固定为 `技术`、`学习`、`随笔`，支持摘要、标签、封面、置顶和草稿字段。
- 已完成本地写作、文章校验、站点检查和发布脚本：`scripts/new-post.js`、`scripts/validate-posts.js`、`scripts/check-site.js`、`scripts/publish.js`。
- 已完成 Decap CMS 中文后台：`source/admin/index.html`、`source/admin/config.yml`、`source/admin/preview.css`。
- 已完成 GitHub Pages 工作流和拉取请求检查：`.github/workflows/pages.yml`、`.github/workflows/validate.yml`。
- GitHub Pages 已启用 GitHub Actions 发布，正式站点已上线：`https://xiaocong612.github.io/xiaocong.github.io/`。
- 已完成 Cloudflare Worker GitHub OAuth 部署：`worker/src/index.js`、`worker/wrangler.toml.example`、`worker/README.md`；Worker 地址为 `https://xiaocong-blog-oauth.xiaocong-blog-oauth-worker.workers.dev`，授权范围固定为 GitHub 公开仓库权限，浏览器不能自行扩大权限。
- 已补齐 OAuth 失败回传：用户取消授权、GitHub 令牌交换失败或未返回令牌时，Decap 弹窗会收到可识别的错误消息并结束等待状态。
- 已完成中文 README、脚手架、关于页和站点设置。
- Windows 下发布脚本已改为直接调用当前 Node 执行 Hexo，避免 `npx.cmd` 的 `EINVAL` 问题。
- 搜索索引使用站内文章路径并会自动创建 `public/` 输出目录；前端会按当前部署根路径请求索引并拼接结果链接。
- 已处理 EJS 未定义局部、Stylus 不兼容 `color-mix()` 和部分 `calc()` 的问题。
- 已将 `/admin/` 设为原样发布的 Decap CMS 静态后台，避免被博客主题页面包裹。
- 已完善发布脚本：只允许 `main` 推送到 `origin`，首次发布会建立 `origin/main` 上游，工作区干净时仍能推送已有提交。
- 已修复搜索根路径请求、搜索结果安全渲染、手机端代码块横向滚动和文章、归档、搜索页面的左右留白。
- 已适配 GitHub Pages 项目站点：`_config.yml` 的 `root` 为 `/xiaocong.github.io/`，CMS 的 `site_url` 与正式站点地址均为 `https://xiaocong612.github.io/xiaocong.github.io/`。
- 已启用 Markdown 内容安全净化：不安全的 HTML、事件属性和 `javascript:` 链接不会进入文章页面；搜索结果以安全 DOM 文本节点渲染，且只接受站内路径。
- 已补齐日常写作细节：含冒号的标题会生成有效 YAML，CMS 的首页副标题会生效，中文阅读时长按中文字符估算，搜索日期优先使用 Hexo 的本地日期格式。
- 已修复 Wrangler 使用非默认配置文件时的入口识别问题：Worker 的 `dev` 和 `deploy` 脚本均显式指定 `src/index.js` 与 `wrangler.toml`，并忽略本地 `worker/wrangler.toml`。
- 已修复正式文章页的 fenced Markdown 代码块消失问题，并新增构建产物回归测试。为保持 Markdown HTML 净化，`marked.dompurify` 仍启用；`syntax_highlighter` 改为空，让 Marked 输出标准的 `<pre><code>` 元素，主题既有样式继续负责显示和横向滚动。

## 已验证

以下检查在本次工作中均通过：

```text
npm test       36 项测试全部通过
npm run check  站点检查和 Hexo 生成通过
npm run build  Hexo 静态构建通过
git diff --check 通过，仅有 Windows 换行符提示
```

已确认生成文件存在且非空：

- `public/index.html`
- `public/2026/09/06/hello-world/index.html`
- `public/css/style.css`
- `public/search.json`
- `public/admin/index.html`
- 归档、分类、标签、关于和搜索页面

生成结果中未发现默认的 `Hello World`、`Landscape`、`John Doe` 等内容。

## 当前预览

项目路径预览入口为：`http://localhost:4003/xiaocong.github.io/`。

已完成首页、文章、归档、搜索、关于和 `/admin/` 页面的桌面、手机验收。页面没有全局横向溢出；搜索“欢迎”能返回文章；`/admin/`、`config.yml` 和 `preview.css` 均可加载。项目站点必须从带 `/xiaocong.github.io/` 的路径访问，裸地址 `http://localhost:4000/` 不是本站首页。

常规 `npm run preview` 只用于根路径下的日常内容和版式查看，不作为项目子路径验收。下一次项目路径验收应先运行 `npm run clean` 与 `npm run build`，再按 `README.md` 中的临时静态挂载命令，将 `public/` 挂载到 `/xiaocong.github.io/`，并访问 `http://localhost:4003/xiaocong.github.io/`。正式站点已上线，后续内容发布后还要在 `https://xiaocong612.github.io/xiaocong.github.io/` 复核站内链接、搜索和图片路径。

## 尚未完成

1. 创建 GitHub OAuth App，将回调地址设为 `https://xiaocong-blog-oauth.xiaocong-blog-oauth-worker.workers.dev/callback`，并将 `GITHUB_CLIENT_ID`、`GITHUB_CLIENT_SECRET` 写入 Cloudflare Worker Secret。
2. 使用正式站点 `/admin/` 完成一次 GitHub 登录和文章草稿发布验收。

### 代码块修复记录（2026-09-12）

- 问题根因是 Hexo 在启用 `highlight.js` 时，将 fenced code block 预处理为 `<hexoPostRenderCodeBlock>`，随后转换为内部 HTML 注释占位符；`hexo-renderer-marked` 的 DOMPurify 会安全地移除该注释，导致 Hexo 无法恢复代码内容。
- 关闭 Hexo 的预处理高亮后，Marked 原生输出标准的 `<pre><code class="language-bash">` HTML，DOMPurify 可安全保留它。没有关闭 `dompurify`，因此文章 HTML、事件属性和危险链接仍会被净化。
- `test/code-block-rendering.test.js` 会先执行 `hexo clean` 再实际构建，并断言文章产物包含 fenced 代码内容；这样不会因 `db.json` 缓存误判通过。本次已运行并通过该测试、完整 `npm test`、`npm run check` 与 `npm run build`。
- 当前主题会显示标准代码块，但不再提供服务端 `highlight.js` 的语法着色、行号和代码标题；日常文章请统一使用 fenced Markdown，不要使用 `{% codeblock %}` 等 Hexo 标签插件。

## 下次建议顺序

1. 读取本文件，检查 `git status`、当前分支和远程地址。
2. 运行 `npm test`、`npm run check`、`git diff --check`。
3. 修改内容时优先使用本地 Markdown 与 `npm run post`；需要可视化编辑时打开 `/xiaocong.github.io/admin/`。
4. 日常预览可以使用 `npm run preview`；项目路径验收则按 README 的静态挂载命令访问 `http://localhost:4003/xiaocong.github.io/`，不要把 `_config.yml` 的 `root` 临时改成 `/`。
5. 创建 OAuth App 并写入两个 Worker Secret 后，打开正式站点 `/admin/` 完成登录和文章草稿发布验收。
6. 不要把 OAuth 密钥写入仓库。

## 当前工作区

- 当前目录：`D:\Code\Blog`
- 发布分支固定为 `main`；发布脚本只接受 `main` 推送到 `origin/main`。
- 目标远程仓库为 `https://github.com/xiaocong612/xiaocong.github.io.git`，已确认仓库公开、默认分支为 `main`。
- 当前实现已推送到 `origin/main`；本地工作区应保持干净，后续内容通过 `npm run post` 或 `/admin/` 新增。
- GitHub Actions 的构建、测试、Pages 构建产物上传和部署均已通过；正式站点已由 GitHub Pages 提供服务。
- 所有项目 Markdown 文档已按中文要求编写；必要的命令、路径、配置键和产品名保留英文。
