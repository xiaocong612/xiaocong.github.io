{
  "name": "xiaocong-blog-oauth-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test ../test/worker.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
